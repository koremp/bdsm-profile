import { render } from '@testing-library/react';

import PowerExchange from 'app/profile/select/power-exchange/page';

test('Profile Select Power Exchange Page', () => {
  render(<PowerExchange />);
});
