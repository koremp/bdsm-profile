import { render } from '@testing-library/react';

import RomanticPreference from 'app/profile/select/romantic-preference';

test('Profile Select Romantic Preference Page', () => {
  render(<RomanticPreference />);
});
