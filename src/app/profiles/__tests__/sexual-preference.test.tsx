import { render } from '@testing-library/react';

import SexualPreference from 'app/profile/select/sexual-preference';

test('Profile Select Sexual Preference Page', () => {
  render(<SexualPreference />);
});
