import { render } from '@testing-library/react';

import MonopolyRelationship from 'src/components/select/MonopolyRelationship';

test('Select MonopolyRelationship Component', () => {
  render(<MonopolyRelationship />);
});